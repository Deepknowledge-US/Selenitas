local config = {}

config.ticks = 200 -- iteraciones

config.xsize = 15  -- Dimensiones del mundo. De momento un plano.

config.ysize = 15

config.num_agents = ''   -- Se está especificando en los ficheros de ejemplo

config.setup = true -- De momento suponemos los botones setup y go como pulsados

config.go    = true

return config