local config = {}

config.ticks = 200 -- iteraciones

config.xsize = 60  -- Dimensiones del mundo. De momento un plano.

config.ysize = 60

config.setup = true -- De momento suponemos los botones setup y go como pulsados

config.go    = true

return config